version: '3'
services:
  libretv:
    image: bestzwei/libretv:latest
    container_name: libretv
    ports:
      - "8899:80"
    environment:
      - PASSWORD=111111
      - HIDE_ADULT_APIS=true # 用户可自定义 true/false
    restart: unless-stopped